import { WorkspaceShell } from "../../../src/components/workspace/shell";
import { requireCurrentWorkspace } from "../../../src/auth/current-workspace";
import { getFixedAssetCenter } from "../../../src/application/workforce/workforce-center-service";
import { createFixedAssetAction, postDepreciationAction } from "./actions";
const rial=(v:bigint)=>`${new Intl.NumberFormat("fa-IR").format(v)} ریال`;
const date=(v:Date)=>new Intl.DateTimeFormat("fa-IR").format(v);
export default async function FixedAssetsPage(){const c=await requireCurrentWorkspace();const assets=await getFixedAssetCenter(c.workspace.id);const cost=assets.reduce((s,a)=>s+a.acquisitionCost,0n),acc=assets.reduce((s,a)=>s+a.accumulated,0n),carry=assets.reduce((s,a)=>s+a.carrying,0n);return <WorkspaceShell title="دارایی‌های ثابت" eyebrow="ثبت دارایی و استهلاک خط مستقیم"><div className="grid gap-4 md:grid-cols-4"><Stat title="تعداد دارایی" value={`${assets.length} مورد`}/><Stat title="بهای تمام‌شده" value={rial(cost)}/><Stat title="استهلاک انباشته" value={rial(acc)}/><Stat title="ارزش دفتری" value={rial(carry)}/></div><div className="mt-5 grid gap-5 xl:grid-cols-[.85fr_1.5fr]"><section className="rounded-3xl border border-slate-200 bg-white p-5"><h2 className="text-lg font-black">ثبت دارایی جدید</h2><form action={createFixedAssetAction} className="mt-4 grid gap-3"><div className="grid grid-cols-2 gap-3"><input name="code" required placeholder="کد دارایی" className="input"/><input name="name" required placeholder="نام دارایی" className="input"/></div><input name="acquisitionDate" type="date" required className="input"/><input name="acquisitionCost" required placeholder="بهای تحصیل (ریال)" className="input"/><input name="residualValue" defaultValue="0" required placeholder="ارزش اسقاط" className="input"/><input name="usefulLifeMonths" required type="number" min="1" placeholder="عمر مفید (ماه)" className="input"/><button className="rounded-xl bg-[#079c93] px-4 py-3 font-black text-white">ثبت دارایی</button></form></section><section className="overflow-hidden rounded-3xl border border-slate-200 bg-white"><div className="border-b p-5"><h2 className="text-lg font-black">دفتر دارایی‌ها</h2></div>{assets.length?<div className="overflow-x-auto"><table className="w-full min-w-[900px] text-right text-sm"><thead className="bg-slate-50 text-xs"><tr><th className="p-4">دارایی</th><th>تحصیل</th><th>بها</th><th>ماهانه</th><th>انباشته</th><th>ارزش دفتری</th><th>استهلاک</th></tr></thead><tbody>{assets.map(a=><tr key={a.id} className="border-t"><td className="p-4"><div className="font-black">{a.name}</div><div className="text-xs text-slate-400">{a.code}</div></td><td>{date(a.acquisitionDate)}</td><td>{rial(a.acquisitionCost)}</td><td>{rial(a.monthlyDepreciation)}</td><td>{rial(a.accumulated)}</td><td className="font-black text-[#087f78]">{rial(a.carrying)}</td><td><form action={postDepreciationAction} className="flex gap-2"><input type="hidden" name="assetId" value={a.id}/><input name="periodDate" type="date" required className="rounded-lg border px-2 py-1.5 text-xs"/><button className="rounded-lg bg-[#0f223d] px-2 py-1.5 text-xs font-bold text-white">ثبت</button></form></td></tr>)}</tbody></table></div>:<div className="p-10 text-center text-sm text-slate-500">هنوز دارایی ثابتی ثبت نشده است.</div>}</section></div></WorkspaceShell>}
function Stat({title,value}:{title:string;value:string}){return <div className="rounded-3xl border border-slate-200 bg-white p-5"><p className="text-xs font-bold text-slate-500">{title}</p><p className="mt-2 text-lg font-black">{value}</p></div>}
