import { WorkspaceShell } from "../../../src/components/workspace/shell";
import { requireCurrentWorkspace } from "../../../src/auth/current-workspace";
import { getPayrollCenter } from "../../../src/application/workforce/workforce-center-service";
import { createEmployeeAction, createPayrollRunAction } from "./actions";
const rial=(v:bigint)=>`${new Intl.NumberFormat("fa-IR").format(v)} ریال`;
const date=(v:Date)=>new Intl.DateTimeFormat("fa-IR").format(v);
export default async function PayrollPage(){const c=await requireCurrentWorkspace();const {employees,runs}=await getPayrollCenter(c.workspace.id);const active=employees.filter(e=>e.active),rows=runs.flatMap(run=>run.lines.map(line=>({run,line}))),total=rows.reduce((s,r)=>s+r.line.netPay,0n);return <WorkspaceShell title="حقوق و دستمزد" eyebrow="مدیریت پرسنل و محاسبه دقیق"><div className="grid gap-4 md:grid-cols-3"><Stat title="پرسنل فعال" value={`${active.length} نفر`}/><Stat title="دوره‌های حقوق" value={`${runs.length} دوره`}/><Stat title="خالص حقوق ثبت‌شده" value={rial(total)}/></div><div className="mt-5 grid gap-5 xl:grid-cols-[1fr_1.35fr]"><div className="space-y-5"><section className="rounded-3xl border border-slate-200 bg-white p-5"><h2 className="text-lg font-black">ثبت پرسنل</h2><form action={createEmployeeAction} className="mt-4 grid gap-3"><input name="employeeCode" required placeholder="کد پرسنلی" className="input"/><input name="name" required placeholder="نام و نام خانوادگی" className="input"/><div className="grid grid-cols-2 gap-3"><input name="nationalId" placeholder="کد ملی" className="input"/><input name="mobile" placeholder="موبایل" className="input"/></div><input name="baseSalary" required placeholder="حقوق پایه (ریال)" className="input"/><button className="rounded-xl bg-[#079c93] px-4 py-3 font-black text-white">ثبت پرسنل</button></form></section><section className="rounded-3xl border border-slate-200 bg-white p-5"><h2 className="text-lg font-black">ثبت دوره حقوق</h2>{active.length?<form action={createPayrollRunAction} className="mt-4 grid gap-3"><select name="employeeId" required className="input">{active.map(e=><option key={e.id} value={e.id}>{e.name} — {e.employeeCode}</option>)}</select><input name="title" placeholder="عنوان دوره" className="input"/><div className="grid grid-cols-2 gap-3"><input name="periodStart" type="date" required className="input"/><input name="periodEnd" type="date" required className="input"/></div><div className="grid grid-cols-2 gap-3"><input name="grossPay" required placeholder="حقوق ناخالص" className="input"/><input name="deductions" defaultValue="0" required placeholder="کسورات" className="input"/></div><button className="rounded-xl bg-[#0f223d] px-4 py-3 font-black text-white">محاسبه و ثبت حقوق</button></form>:<p className="mt-3 text-sm text-slate-500">ابتدا یک پرسنل ثبت کنید.</p>}</section></div><section className="overflow-hidden rounded-3xl border border-slate-200 bg-white"><div className="border-b p-5"><h2 className="text-lg font-black">لیست حقوق</h2></div>{rows.length?<div className="overflow-x-auto"><table className="w-full min-w-[760px] text-right text-sm"><thead className="bg-slate-50 text-xs"><tr><th className="p-4">پرسنل</th><th>دوره</th><th>ناخالص</th><th>کسورات</th><th>خالص</th></tr></thead><tbody>{rows.map(({run,line})=><tr key={line.id} className="border-t"><td className="p-4 font-bold">{line.employee.name}</td><td>{date(run.periodStart)} تا {date(run.periodEnd)}</td><td>{rial(line.grossPay)}</td><td>{rial(line.deductions)}</td><td className="font-black text-[#087f78]">{rial(line.netPay)}</td></tr>)}</tbody></table></div>:<div className="p-10 text-center text-sm text-slate-500">هنوز دوره حقوقی ثبت نشده است.</div>}</section></div></WorkspaceShell>}
function Stat({title,value}:{title:string;value:string}){return <div className="rounded-3xl border border-slate-200 bg-white p-5"><p className="text-xs font-bold text-slate-500">{title}</p><p className="mt-2 text-xl font-black">{value}</p></div>}
